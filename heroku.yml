setup:
  addons:
    - plan: heroku-postgresql
      as: PSQL
  config:
    APP_NAME: eliefere
build:
  docker:
    web: Dockerfile
  config:
    NODE_ENV: production
    PORT: 3333
run:
  web: node dist/main
